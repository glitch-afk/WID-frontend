import Link from 'next/link'

const WidButton = () => {
  return (
    <button
      className={`inline-flex items-center justify-center rounded-md bg-gradient-to-r from-wid-indigo to-wid-purple px-6 py-2 font-medium text-white`}
    >
      Claim ID
    </button>
  )
}

export default WidButton
